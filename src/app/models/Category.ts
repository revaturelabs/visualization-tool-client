export interface Category {
    categoryId: number;
    categoryName: string;
    categoryDescription: string;
    categoryColor: string;
}

export interface CategoryDTO {
    categoryName: string;
    categoryDescription: string;
}
